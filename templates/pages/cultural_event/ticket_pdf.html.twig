<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{ event.title }}</title>
    <style>
        
    </style>
</head>
<body>
    <h1>{{ event.title }}</h1>
    <p>Détails de l'événement : {{ event.description }}</p>
    <p>Nombre de tickets : {{ numberOfTickets }}</p>

    {% for i in 1..numberOfTickets %}
        <div class="ticket">
            <h2>Ticket {{ i }}</h2>
            <p>Informations supplémentaires ici...</p>
        </div>
    {% endfor %}
</body>
</html>
