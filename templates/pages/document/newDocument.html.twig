{# templates/pages/document/newDocument.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}<i class="fa-solid fa-file-circle-question"></i> Ajouter un Document{% endblock %}

{% block body %}
    <h1>Ajouter un Document</h1>

        <a href="{{ path('app_user_documents') }}"><i class="fa-solid fa-arrow-left-long"></i> Retour à mes Documents</a>

    {% for label, messages in app.flashes %}
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                {{ message }}
            {% endfor %}
        </div>
    {% endfor %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Ajouter</button>
    {{ form_end(form) }}

{% endblock %}
