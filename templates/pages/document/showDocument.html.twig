{% extends 'base.html.twig' %}

{% block title %}<i class="fa-solid fa-folder-open"></i> Voir Document {{ document.title }}{% endblock %}

{% block body %}

    <h1>{{ document.title }}</h1>

    <iframe src="{{ asset(filePath) }}" width="100%" height="600" style="border: none;">
        <p>Il semble que votre navigateur ne soit pas configuré pour afficher les fichiers PDF. 
            Pas de souci, cliquez ici pour <a href="{{ asset(filePath) }}">télécharger le fichier PDF.</a>
        </p>
    </iframe>


    <br>
    <a href="{{ path('app_document_edit', {id: document.id}) }}" class="btn btn-warning">Éditer</a>
    <a href="{{ path('app_document_delete', {id: document.id}) }}" class="btn btn-danger" onclick="return confirm('Êtes-vous sûr de vouloir supprimer ce document ?')">Supprimer</a>
    <a href="{{ path('app_document_download', {id: document.id}) }}" class="btn btn-success">Télécharger</a>

    <br><br>
    <a href="{{ path('app_user_documents') }}" class="btn btn-secondary">Retour à mes documents</a>

{% endblock %}
